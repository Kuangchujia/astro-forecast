<?php
/**
 * Plugin Name:       嘉言一得天象历法（KuangChujia 天象预报模块）
 * Plugin URI:        https://kuangchujia.com/sky-forecast/
 * Description:        天象预报模块（今日天象 / 未来预告 / 历史回推）。后端预计算 + 静态/半静态渲染；短代码 [astro_today] [astro_forecast_list] [astro_related_events] [astro_history_today] [astro_forecast_report]；CPT astro_event + 分类法 event_type；REST 导入端点（WordPress.com 不开放外部 MySQL，须经此入库）。数据表 wp_astro_daily / wp_astro_events / wp_astro_relations / wp_astro_daily_site 激活时自动建。
 * Version:           2.3.16
 * Requires at least: 5.8
 * Requires PHP:      7.4
 * Author:            作者：「嘉言一得（邝楚嘉）」Author: Chujia Kuang
 * License:           GPL-2.0-or-later
 * License URI:       https://www.gnu.org/licenses/gpl-2.0.html
 * Text Domain:       kcj-astro
 *
 * ── 目录结构（对应《WordPress 站点天象模块落地指令集》指令 01）──────────
 *   includes/   数据层（class-astro-db.php）、CPT 与分类法（cpt.php）、短代码（shortcodes.php）、
 *               REST 导入（rest-import.php）、定时任务（cron.php）、合规（compliance.php）、
 *               SEO/结构化数据（rankmath.php）、激活（activation.php）
 *   assets/     样式（astro-style.css）
 *   templates/  前端模板（今日天象 / 预告列表 / 事件详情 / 关联 / 单页 / 归档）
 *   data/       导入用 JSON 数据（结构、样例与用法；大数据集不进 zip）
 *
 * ── 加载顺序有依赖，不要随意调整 ──────────────────────────────────
 *   class-astro-db.php 必须最先（别的模块都要用它取表名）；
 *   cpt.php 的注册走 init 钩子（不是激活钩子，见 v1.2.0 说明）；
 *   cron.php 用 KCJ_Astro_DB 与 kcj_astro_forecast_flush_cache()，故排在 activation 之后。
 *
 * ── 硬性约束 ────────────────────────────────────────────────────
 *   全站禁占星/运势/吉凶/谶纬；统一北京时间 UTC+8；历史三层（文献记载/现代回推/史料辨析）
 *   由模板分支渲染；不得虚构，不得为不可见内容声明结构化数据。
 *
 * ── v1.2.0（落地指令集执行版）变更摘要 ───────────────────────────
 *   新增：CPT 与分类法在 init 上注册（修 404 必崩）、元字段、模板注入、旧 URL 301、
 *         定时任务与新鲜度审计、the_content 免责声明与可选自动内链、
 *         Rank Math 适配（Schema 分工）、data/ 目录、build_dataset.py 配套。
 *   修正：URL 前缀 sky/forecast → sky-forecast（指令 04）；表结构补
 *         special_event_ids / created_at / obs_site 与 relations 唯一键（幂等）。
 *   详见 docs/instruction-review.md 与 docs/execution-report.md。
 *
 * ── v1.2.1（线上首推修复版，2026-09-23）变更摘要 ───────────────────
 *   修正：`daily.data_version` 与 `events.dt_model` 列宽 32 → **64**（F22）。
 *         v1.2.0 这两列装不下 `de421+dt_espenak_meeus_2006+tt_scale`（36 字符，
 *         解析降级行 45 字符）⇒ 线上导入被 MySQL 拒收，回执 `written:0, failed:1`。
 *   新增：**KCJ_ASTRO_SCHEMA** 表结构版本常量 + `init` 上的 dbDelta 增量升级守卫
 *         ⇒ 已建库站点升级插件后**无需重新激活**，首次访问即自动 ALTER。
 *
 * ── v1.3.0（结构化数据归属修正版，2026-09-23）变更摘要 ─────────────
 *   背景：79 条事件详情页**整页零 JSON-LD**（连 Person / WebSite 都没有），
 *         面板里把 CPT 默认 Schema Type 选成 Event 无效 —— 根因在 Rank Math 免费版源码的
 *         白名单（只认 Article 家族），详见 includes/rankmath.php 文件头 ④。
 *   修正一：**Event 改由本插件输出**（原以为交给 Rank Math 面板即可）。
 *   修正二：`rank_math/json_ld` 回调优先级 **20 → 5**，抢在 Rank Math 的
 *         `add_context_data`（默认 10）之前放入节点 ⇒ 使 `! empty( $data )` 成立，
 *         事件页的**站点级实体（Person / WebSite / ImageObject / WebPage）随之恢复输出**。
 *   修正三：daily 上下文**必须先取到日期**才建节点（原会输出日期为空的 Dataset，
 *         线上表现为 name「揭阳每日天象数据集（）」、@id 以 `-` 结尾）。
 *   新增：**php_selftest.py**（语法闸 `php -l` + WordPress 钩子桩测试 30 项，含负控制）
 *         —— 此前本项目**没有任何 PHP 层判据**，插件代码从未被执行过一次。
 *   表结构未变 ⇒ KCJ_ASTRO_SCHEMA 仍为 2，升级**无需重新激活**。
 *
 * ── v2.0.0（观测地可选 / 三子项版，2026-09-23）变更摘要 ─────────────
 *   背景：用户要求把「天象预告」做成父项、下设三个子项 ——
 *         子1 今日天象（**观测地默认为 IP 当地并可选**）、
 *         子2 未来天象预告（月/季/年度报告，**供下载**）、
 *         子3 历史上今日天象（页面展示 + 汇总报告**供下载**）。
 *   新增一：**第 4 张表 `astro_daily_site`**（观测地维度）＋ `python/build_site.py`
 *         ＋ `compute_sky.compute_site_daily()` ＋ 38 城预置档（原 10 城）。
 *         为什么拆表：daily 一条记录 40 余项里只有 11 项与观测地有关（升落与晨昏），
 *         把 daily 乘上城市数会爆；拆表后每行不到 200 字节。
 *   新增二：`[astro_today]` 增 **place 属性**（auto | 城市键 | off）＋ 模板内
 *         **观测地选择器**＋ `assets/astro-place.js`（IP 定位用免费的公开接口，
 *         失败即静默回落默认观测地；**前端仍零天文计算**，只换预计算值）。
 *   新增三：`[astro_history_today]` —— 历史上同月同日的日月食（页面展示）。
 *   新增四：`[astro_forecast_report]` —— 月/季/年度报告，**页面内即时生成并下载**
 *         （Markdown ＋ 可打印 HTML），零后端、零凭据。
 *   新增五：`[astro_forecast_list]` 增 **side 属性**（future | past | all）——
 *         历史回推的日月食与未来预告同属 `lunar_eclipse` / `solar_eclipse`，
 *         不加这一层过滤，1900 年的月食会混进「未来预告」列表。
 *   修正一（F27）：`_rise_set` / `_twilight` 的搜索窗口由 **UTC 日**改为**北京日**。
 *         原口径下「当日日出」实为**次日**日出；且乌鲁木齐(87.62°E)、拉萨(91.13°E)
 *         的日出整个落在窗外 ⇒ **静默返回 None**（加 38 城后第一次真跑即暴露）。
 *   修正二（F28）：升落显式取地平线 **−0.8333°**（折射 34′ ＋ 太阳视半径 16′）。
 *         skyfield 默认是 −34′＝−0.5667°（只含折射），比公开历表晚约 1.4 分钟。
 *         实测对照：北京 06:03:55→**06:02:31**（公开 06:01—06:02）、
 *         乌鲁木齐 07:58:55→**07:57:27**（公开 07:57—07:58）。
 *   表结构变更（新增第 4 张表）⇒ **KCJ_ASTRO_SCHEMA 升为 3**，
 *         已建库站点升级插件后首访即由 init 守卫自动建表，无需重新激活。
 *
 * ── v2.2.0（2026-09-23）：把「数据导入」搬进后台 ────────────────────────
 *   缘起：数据原先只有「外部进程 → 鉴权 REST 端点」一条通路，而线上拿不到有效凭据
 *         （站点**支持**应用密码，根索引里宣告了该端点；但外部拿不到站点级口令，
 *          且 WordPress 生成的口令是 24 位纯字母数字，用户手上那串含特殊字符、不是应用密码），
 *         结果三栏目长期空着。现改为**数据随包上传、后台点一次即入库**，
 *         不依赖计划类型、不依赖任何令牌。
 *   F51  报告模板**没有任何期间切换 UI** ⇒ 需求要的「月/季/年度」实际只达 1/3。
 *        现于模板内加 `?kcj_period=` 链接条（纯链接、零脚本，与历史栏 `?kcj_md=` 同一成法），
 *        astro_hub 的默认期间由 month 改为 next12（「未来天象预告」读者要看的是接下来有什么）。
 *   F52  kcj_astro_sync_event_to_post 不按 slug 认领已有文章 ⇒ 同一 slug 二次同步会因
 *        post_name 撞车被 WordPress 自动改成 `-2`/`-3`，**静默**产出重复页面。现先认领再新建。
 *   配套（数据层，见 make_datasets.py 的裁定）：
 *        历史 4,672 条（1900—2025）以 publish_status=0 入库、**不建详情页** ——
 *        它是数据集不是「已发布文章」，于是归档页/结构化数据/站点地图完全不受污染；
 *        「历史上今日天象」栏的查询已同步改为不看 publish_status（该栏是页面内联渲染）。
 *        未来 145 条以 publish_status=1 入库并建详情页。
 *   无表结构变更 ⇒ **KCJ_ASTRO_SCHEMA 仍为 3**。
 *
 * ── v2.2.1（2026-09-23）：历史栏空态措辞改准 ──────────────────────────
 *   缘起：v2.2.0 把历史数据（4,672 条）按「数据集」入库（publish_status=0、不建页），
 *         而历史栏的空态文案还写着「暂无**已发布**的天象条目」—— 读者会理解成
 *         「是没发布才看不到」，与真实原因（该日确无录入 / 数据未导入）不符。
 *   修正：`templates/astro-history-today.php` 空态改为「这一天暂无天象条目。」，
 *         文件头注释同步注明「本栏不按 publish_status 过滤」。
 *   说明：`astro-forecast-list.php` 与 `astro-forecast-report.php` 里的「已发布」
 *         **保持不变** —— 那两处确实只读 publish_status=1 的行，措辞无歧义。
 *   无表结构变更 ⇒ **KCJ_ASTRO_SCHEMA 仍为 3**。
 *
 * ── v2.2.2（2026-09-23）：导入器的四处「失败不发声」 ────────────────────
 *   缘起：用户报「已全部完成」，而线上「今日天象」仍降级、「历史今日」仍全空。
 *         外部取证发现两头对不上 —— events_future（145 行，在清单里排在 daily_site
 *         **之后**）已完整入库（报告按未来 12 个月过滤后正好显示 62 条，逐字吻合）；
 *         而 daily_site（29,298 行）在页面上已被算作「完成」，却查不到 2026-09-23
 *         （该日期在数据件第 14,061 行 ＝ (2026-09-23 − 2025-09-18) × 38 + 1）。
 *   四处根因（共同点：出错不发声）：
 *     F53 **写入失败不中断、游标照推** ⇒ 目标表不存在或列不匹配时，页面显示
 *         「已完成 100% ＋ 成功 0 / 失败 29298」，**看上去像成功**。
 *         现：进 `kcj_astro_import_run()` 先做**目标表存在性前置检查**，缺表即刻报错；
 *         `failed > 0` 以 `notice-error` 显性呈现失败行数与前三条原因；
 *         并加「补建 / 升级表结构」按钮（`kcj_run=repair`）—— 当场 dbDelta ＋ 实测复验，
 *         不再只靠 init 守卫（守卫一旦因 option 被误写而停止重试，表就永远缺着）。
 *     F54 页面上看不到「表在不在」⇒ 新增**表结构自检区**：4 张表逐张列存在性与现存
 *         行数，使「表不存在」与「表存在但空」肉眼可分。
 *     F55 `kcj_astro_import_read()` 每跳都从文件头 fgets 到 offset ⇒ 对 29,298 行的
 *         数据件是 **O(n²)**，越跑越慢、最后像卡死。现游标增记**字节位 pos** ＋ 数据件
 *         字节数 size：一致则 fseek 直读；不一致（数据件被换过）则**从第 1 行重导**并
 *         如实报出，绝不静默错位。分块 120 → 400，跳数再降一个量级。
 *     F56 游标按「成功行数」推进（`$offset += count($rows)`）⇒ 文件里只要有 1 行非法
 *         JSON，该行永远重读（死循环）；兜底 `if (!$rows) { $offset = $total; }` 更糟，
 *         会**静默跳过后面所有行**却报「已完成」。现改按 **consumed（消费行数）** 推进，
 *         兜底收窄为「一行都没消费到」。
 *   无表结构变更 ⇒ **KCJ_ASTRO_SCHEMA 仍为 3**。
 *
 * ── v2.2.3（2026-09-23）：行序改「离今天近优先」＋ 数据件 sha1 指纹 ─────────
 *   缘起（由上一条的取证直接推出来的）：v2.2.2 修好「失败不发声」之后，外部取证
 *         确认包已是 2.2.2、数据表却仍空；再等 9 分钟仍**逐项一字未变**。
 *         停在这里会得出「导入没跑」的错结论。真正的原因是**导入在跑，只是行序不对**：
 *      · 导入是**逐跳**推进的（每跳 400 行 / 18 秒，靠 meta refresh 自动续跑），
 *        故**行序 = 数据到达页面的先后**；
 *      · 而数据件原是**时间升序**：`daily_site` 的「今天」在第 **14,061** 行
 *        （＝ (2026-09-23 − 2025-09-18) × 38 + 1），「未来预告」要的事件件排在
 *        它**之后**（第 29,299 行起）⇒ 前 35 跳读的全是与今天无关的日期；
 *      · 于是「点了导入、等 9 分钟、页面没反应」是**必然**，
 *        而用户合理地判成「已完成 / 坏了」。
 *   修法（数据层，见 make_datasets.py）：
 *     W1 行序改「**离今天近优先**」。daily / daily_site 按与今天的天数距离升序
 *        （同距离未来优先）；events_past 按**月日的环形距离**升序（同月日内年份倒序，
 *        与页面 `ORDER BY jd_core DESC` 一致）；events_future 时间升序本身即由近及远。
 *        ⇒ 第一跳就写入「今天那 38 行」与「本日的历史条目」，
 *          今日栏与本日历史栏**几秒内**即正常，不必等全量跑完。
 *     W2 清单 `_manifest.json` 的每个数据集增 `sha1` 与 `order` 两字段，
 *        顶层增 `anchor_date`（行序锚点）。
 *   修法（导入器，本版本代码侧）：
 *     W3 导入前的守卫从「比**字节数**」升级为「比 **sha1**」。
 *        ★ 这是必须的，不是加保险：**重排行序不改变文件字节数** ——
 *          实测四个数据件重排前后字节数**一字节不差**
 *          （2,325,814 / 13,201,509 / 193,484 / 7,821,702），
 *          而 sha1 全部改变。只比字节数 ⇒ 判不出变更 ⇒ 沿用旧字节位落在**行中间**
 *          ⇒ fgets 读回半行 ⇒ JSON 解析失败被记成坏行 ⇒ **后半段静默丢失**，
 *          且游标照常推进、页面照常报「已完成」。upsert 幂等，故重导只花时间、不重复行。
 *     W4 数据件 sha1 与**清单声明值**不符 ⇒ 当场拒跑并说清（多半是上传不完整，
 *        或清单与数据件不是同一次生成）。宁可不动手，也不许写进一批来历不明的数据。
 *     W5 后台页新增「**数据件自检**」区：每件列出 行序 / **第 1 行是谁** / sha1 /
 *        与清单是否一致，并显示行序锚点。把「行序错了、包没换」这两件
 *        原先**在外面完全看不出来**的事，变成页面上的一行字。
 *   无表结构变更 ⇒ **KCJ_ASTRO_SCHEMA 仍为 3**。
 *
 * ── v2.2.4（2026-09-23）：**「点了却什么都没发生」的真正成因** ─────────────
 *   缘起：v2.2.3 上线后，外部取证**已确认包与数据件都对了** —— CSS 标记 2.2.3、
 *         页面资源 `?m=` 已变；线上清单 `anchor_date=2026-09-23`、四个数据件的
 *         `order`/`sha1` **与本地逐一相同**；线上数据件首行确已是 2026-09-23
 *         （甲/丙/戊 三组 **12 项全过、0 失败**）。**而三栏仍一字未变**
 *         ⇒ 只剩一种解释：**导入这一步没真正发生**。回读代码，找到四个成因，
 *         全在「一键全部」这条路上（也正因如此，两个栏目同时空着，却看不出联系）。
 *
 *   F57 **队头阻塞**：原实现只取「第一个未完成的集」当 target，它一报错就
 *        `return WP_Error` ⇒ **整条链中断** ⇒ 排在它后面的集**永远轮不到**。
 *        而 `daily_site` 在清单里排第 2 位，又恰是最可能报错的那个（表缺或列不匹配）
 *        ⇒ `events_past`（4,672 行）**从未被导入** ——「历史栏一直空」与
 *        「今日栏一直降级」其实是**同一个**原因造成的，而页面上只有一句红字，
 *        看不出「排在它后面的全被挡住了」。
 *        ⇒ 现改为：**报错的集跳过、继续下一个**，并把每个集的错误分别列出。
 *          （每跳都会再撞一次那个错、于是重复显示同一条红字 —— 有意为之：
 *           宁可啰嗦，也不许把「某个集一直没进去」这件事藏起来。）
 *
 *   F58 **dbDelta 认不出的 DDL 会被它静默跳过**：dbDelta 不是「照 SQL 执行」，
 *        而是**先解析** DDL 再与现状比对；解析器认不出的部分它**不报错、只是不做**。
 *        于是会出现「4 张表里 3 张建好、第 4 张永远缺着」，而两头都看不到原因：
 *          · init 守卫那边：`width_shortfalls()` 因「列读不到」判不合格 ⇒ 不写 schema
 *            option ⇒ **每次请求都白跑一遍 dbDelta**（永远重试、永远不成功）；
 *          · 页面那边：只显示「仍缺失」，用户转述不出原因 ⇒ 外部只能靠猜（本轮猜了两轮）。
 *        ⇒ `KCJ_Astro_DB::create()` 增**直连 `CREATE TABLE IF NOT EXISTS` 兜底**：
 *          只对「确实不存在」的表用（已存在的表不动，列宽升级仍归 dbDelta），
 *          并把 `$wpdb->last_error` **记进 schema_error、并显示在页面上**
 *          （没有原始错误时，「建不出来」这句话在站外等于没有信息）。
 *
 *   F59 **每跳只推进一个集 ⇒ 先到的不一定是先要的**：原实现每跳只跑一个集，
 *        于是「先跑完 `daily_site` 的 74 跳，历史栏才轮到」——按「每行一次查询 ＋
 *        一次写」估算，那是 **20 分钟以上**。
 *        ⇒ 现改为**每跳把每个未完成的集各推进一跳**，总时间盒**按剩余集数分摊**
 *          （`kcj_astro_import_run($key, $budget)`）。★ 分摊这一步不能省：
 *          不分摊的话第一个集独自吃掉整个 18 秒 ⇒ 后面的集本轮根本没机会跑 ⇒
 *          「每跳各推进一跳」会**静默退化**成原样（看着改了、其实没改）。
 *
 *   F60 **打开导入页不自动开始**：这一步原先要求「先点补建、再点一键全部」，
 *        任一环节错位就什么都看不到，且页面上没有线索可自查 —— 用户两次答
 *        「已执行」，两侧始终对不上。而本页的**唯一用途就是导入** ⇒ 改为
 *        **一打开就自动开始 / 续跑**。三道护栏：页面本身已过
 *        `current_user_can('manage_options')`；显式带 `kcj_run` 时**照旧校验 nonce**；
 *        **数据件 sha1 与清单不符时不自动开始**（那种情况下开跑会写进来历不明的数据）。
 *
 *   连带（数据层，见 make_datasets.py）：**SETS 顺序**改为「第一跳就能点亮某一栏」
 *        的代价由小到大 —— `events_past` → `daily` → `daily_site` → `events_future`。
 *        因为「一键导入」的推进顺序**就是清单顺序**，所以**顺序也是功能**。
 *   无表结构变更 ⇒ **KCJ_ASTRO_SCHEMA 仍为 3**。
 *
 * ── v2.2.5（2026-09-23）：**公开状态信标** —— 让「导入有没有跑」在站外可读，免凭据 ──────
 *   缘起：v2.2.4 上传并生效之后（甲组 CSS 标记 2.2.4、?m= 已变；丙组清单顺序与四件
 *         sha1 与本地逐一相同；戊组三条首行全对 ⇒ 13 项全过），**三栏仍一字未变**
 *         （仍需导入 daily_site、仍「一条都没收录」、历史栏数据岛 0）。也就是说：
 *         包对了、数据对了、行序对了，而**「导入这一步究竟有没有发生」我依然看不见**。
 *         `/health` 与 `/import` 都要 `edit_posts`（外网实测 401）⇒ 免凭据观察后台
 *         这条路**原本根本不存在**，只能靠页面上的几句文案反推 —— 本轮因此猜错两回。
 *
 *   本版补上这条路：把运行状态写成**公开可读**的两个文件（刻意放在 uploads/，
 *   **不在插件目录** —— 那里会被 make_zip 打进包、且 WP.com 上常常只读）：
 *     · wp-content/uploads/kcj-astro-status.json   最近一次完整快照（缩进 JSON，覆盖写）
 *     · wp-content/uploads/kcj-astro-log.jsonl     每次调用追加一行（滚动保留最近 200 行）
 *   内容只有**计数、游标、失败行数、错误文本、行序与 sha1**；不含凭据与个人信息。
 *   触发面刻意做宽：**只要「数据导入」页被打开就写一次**（连「什么都没点」也写）——
 *   因为「文件没出现」恰好能区分「页面根本没打开」与「打开了但导入坏了」，
 *   而这两种情形过去在站外**完全同形**。
 *   另有 `activation.php` 在**启用时**也写一次 ⇒ 「上传并启用」这件事立刻可见。
 *
 *   ★ 设计纪律（皆为已踩之坑）：① 写盘失败**不得静默** —— 记进 option
 *     `kcj_astro_beacon_error` 并在导入页显示（否则「有信标」与「没写出来」同形）；
 *     ② 不写进插件目录（会污染打包判据）；③ **逐次出声**，不依赖定时任务；
 *     ④ 日志**滚动**，不无限增长；⑤ 信标自身不得成为故障源 —— 取值一律
 *     `function_exists` / `class_exists` 守卫，拿不到就记 null、绝不抛错。
 *   无表结构变更 ⇒ **KCJ_ASTRO_SCHEMA 仍为 3**。
 *
 * ── v2.2.6（2026-09-23）：列宽按**字节**算才够；且此类失败从此自带数字 ────────────
 *   病根 F27：信标第一次给出确凿读数 —— 四集游标全 100%，而
 *     `events_past 4672/4672 failed=4672`，原文
 *     「处理以下字段的值失败：time_uncertainty。提供的值可能太长或包含无效数据。」
 *   逐列量真值后得到：`time_uncertainty` 最长 **60 字符 / 159 字节**，而列声明 VARCHAR(64)
 *   —— **字符数够、字节数不够**。WordPress 的 `wpdb::strip_invalid_text()` 在字符集为
 *   单字节（latin1）或值恰为纯 ASCII 时**按字节**截断，截断即判该行写入失败。
 *   （依据：wp-includes/class-wpdb.php —— 本轮直接取核心源码逐行核对，不是推的。）
 *
 *   四处同批修（缺一即复发）：
 *     ① `events.time_uncertainty` **VARCHAR(64) → VARCHAR(191)**（191 ≥ 159 字节、≥ 60 字符，
 *        两种口径同时通过；并沿用 slug / obs_site 的既有宽度口径）⇒ **SCHEMA 3 → 4**。
 *     ② `min_widths()` 纳入该列 —— 事故列原先**不在监视面内**，所以「列宽达标」是真话、
 *        却覆盖不到真正出问题的那一列。
 *     ③ 新增 `includes/col-budget.php`：写入前用 WordPress **自己的**两个函数
 *        （`get_col_length()` / `get_col_charset()`）量长度，超限时报
 *        「字段 / 值长（字符＋字节）/ 上限 / 口径 / 实际列类型」。
 *     ④ 表结构变更后**自动重导上次失败的数据集**（原先标着「已完成」就永不再碰，
 *        必须人工点「重跑」—— 这使「修好列宽」与「数据真正进库」之间还差一次人工动作）。
 *   另修：前台短代码把「空态」也缓存 12 小时（负缓存）⇒ 数据到位后页面仍说「需导入」。
 *   无新增表、无列删除 ⇒ 仅**加宽一列**，属向后兼容的增量升级。
 *
 * ── v2.2.7（2026-09-23）：栏目激活态随 URL 走（F52）─────────────────────────────
 *   用户线上报：「在未来天象中选择：本月、本季、本年，都会自动跳转到今日天象。
 *   在历史天象中选择：前一天、前七天、后一天、后七天，都会自动跳转今日天象。」
 *
 *   病根：`[astro_hub]` 三栏是**纯 CSS radio 切换**，而模板把**第 0 个** radio
 *   写死成 checked（`($i === 0) ? ' checked="checked"' : ''`）。栏内每个跳转链接
 *   都是普通 `<a href>`（未来栏换期间、历史栏换日子）⇒ 一定整页重载，
 *   而 radio 状态不随页面保留 ⇒ 永远复位到第 0 栏。
 *   ★ 要注意这一类缺陷的**性质**：它既不是数据错也不是报错，数据其实**已经换对了**，
 *     只是被藏在第 2、3 栏里。用户看到的是「点了没反应」
 *     —— 比真报错更难查（没有报错可查），也比真报错更伤（读者会以为功能没做）。
 *
 *   修法（两处，缺一即复发）：
 *     ① 新增纯函数 `kcj_astro_hub_active_key()`：按 URL 参数算出该开哪一栏
 *        （`?kcj_tab=` 显式指定 > `?kcj_period=` → future > `?kcj_md=` → past > 第一栏）。
 *        抽成函数是为了**可单测**（留在闭包里则桩测试碰不到）。
 *     ② 各栏自己的跳转链接**自带 `kcj_tab`**（期间链接带 future、历史链接带 past）
 *        ⇒ 新链接**永不歧义**，② ③ 只是旧链接的兜底。
 *
 *   ★ 被否掉的方案（记下来，防以后有人重新想一遍）：
 *     「用脚本拦掉跳转、只切栏不重载」。**那是错的** —— 本页各期间/各日的**内容**
 *     由服务端按参数查询后渲染，不重载就换不出来；拦住跳转只会让地址栏与页面内容
 *     不一致，把「看起来没生效」升级成「真的没生效」。
 *     正确方向是**让重载后停在原栏**，不是不重载。
 *   无表结构变更、无新表 ⇒ **KCJ_ASTRO_SCHEMA 仍为 4**。
 *
 * ── v2.3.2（2026-09-23）：修 F62（「天象预告」页丢掉 Dataset 结构化数据）──────────
 *   **症状（线上探针 H 项报红，不是用户报的）**：
 *     期望出完好 Dataset 的页：/tianxiang-yugao/ ／ 实际命中 0 个
 *     同一时刻直采旁证：首页（用 [astro_today]）Dataset **1** 个；
 *     「天象预告」页 hub 容器 38 处、data-kcj-section="today" 1 处、Dataset **0** 个。
 *   **根因**：daily 判据只认字面 `[astro_today]`。而该页用的是 `[astro_hub]` ——
 *     它的「今日天象」栏**默认开启**（today="1"），且三栏都会渲染进 HTML
 *     （模板是 radio + CSS 切换，不是 JS 按需取数）⇒ 页面上确实有今日天象内容，
 *     结构化数据却是 0。页面可见内容与结构化数据声明不一致。
 *   **修法**：抽纯函数 `kcj_astro_page_today_date($raw)`，认三种承载方式 ——
 *     ① `[astro_today date="YYYY-MM-DD"]`（显式日期优先）② 裸 `[astro_today]`
 *     ③ `[astro_hub]` 且其 today 开关**严格等于字符串 '1'**。
 *     第 ③ 条的严格性是刻意的：与 shortcodes.php 的 `$atts['today'] === '1'` 逐字对齐，
 *     否则用宽松判据会出现「页面上没这一栏、却声明了数据集」的反向失真。
 *   **判据**：php_selftest.py 新增 hub_page（正例）＋ hub_page_today_off（负控制），
 *     全套 43 → **48** 项；对旧代码复现为 2 条红（plugin_types=[]），修后 **48/48**。
 *   **顺带修桩两处**（症状都是「加了用例、却报出与缺陷无关的错」）：
 *     ① `is_singular()` 的排除名单改成字面清单 —— 新增 case 必须登记，
 *        否则被误判成 CPT 单篇，跑出 Undefined array key 之类的假故障；
 *     ② `get_row()` 的 daily 行改**按 SQL 形状认**（原先按 case 名认 ⇒ 新用例取不到行）。
 *   无表结构变更 ⇒ **KCJ_ASTRO_SCHEMA 仍为 4**。
 *
 * ── v2.3.1（2026-09-23）：修线上致命错误 F61（导入端点整个 500）──────────────
 *   **症状（用户截图的站点健康页）**：
 *     Fatal error: Call to a member function prepare() on null
 *     in wp-content/plugins/wp-astro-forecast/includes/rest-import.php
 *     栈：#0 rest-import.php(223): kcj_astro_rest_bulk_upsert(Array, Array, NULL)
 *         #1 admin-import.php(330): kcj_astro_rest_upsert('daily_site', …)
 *   **根因**：v2.3.0 新增的多行快路径把语句拼装写在 `$flush` **闭包**里，
 *     闭包内用了 `$wpdb`，而 **PHP 闭包不继承任何外层作用域** ——
 *     函数顶部那句 `global $wpdb;` 到不了闭包里面，闭包内 `$wpdb` 是未定义变量
 *     （null），首次 `$wpdb->prepare()` 即致命错误 ⇒ **导入端点整个 500**。
 *     ⇒ 修法：在闭包体内**再声明一次** `global $wpdb;`。
 *       不用 `use ($wpdb)`：capture-by-value 抓的是**定义那一刻**的值，
 *       将来初始化顺序一变就会静默捕获 null —— 同一故障的第二次。
 *   **为什么九闸全绿还是上了线**：`php_selftest.py` 的 WordPress 桩里
 *     **从未调用过这条快路径**（桩 WPDB 连 `query()` 都没有）——
 *     「判据没覆盖」≠「判据通过」。本次把该路径**真跑**纳入常驻判据
 *     （新用例 `bulk_upsert`，并对其做**负控制**：撤掉修复即报红）。
 *   **同类隐患静态闸**：`_diag/scan_closure_scope.py` 扫「函数/闭包体内用了
 *     `$wpdb`、却没在本体取得它」—— 全插件 21 个 `*.php` 现为 **0 命中**
 *     （负控制可报出，仪器本身的行号/重复报数两处 bug 已修）。
 *   无表结构变更 ⇒ **KCJ_ASTRO_SCHEMA 仍为 4**。
 *
 * ── v2.3.0（2026-09-23）：观测地扩到全国 ＋ 界面去重与自适应 ＋ 审计留史 ─────────
 *   用户令三条：
 *     ①「观测地 —— 我的定位是深圳。要精确到全国各个市、县/区，并可选，
 *        按访问位置选**最近的预置观测地**。」
 *     ②「UI 界面需要调整优化，现在太难看了，最好能电脑与手机自适应。
 *        而且**重复的内容过多**。」
 *     ③「/tianxiang-yugao/ 曾瞬时降级：审计只留最后一次观测值，取不到历史
 *        ⇒ 待办『改为保留近 N 次』。」
 *
 *   ① 观测地：预置档由 **38 城 → 340 个地级锚点**（`python/build_places.py` 采集，
 *      来源＝阿里 DataV GeoAtlas 政府驻地中心点 ＋ opentopodata 海拔），
 *      另有 **2,870 个县级可选点**就近绑定到锚点。
 *      · **老键一律保留**（`jieyang` / `wulumqi` 这类已在历史数据件与历史 URL 里），
 *        且 key、**坐标**、海拔三样都锁回人工校订档 —— 首版只锁 key 与海拔，
 *        实测把揭阳挪了约 2 km；对升落只有秒级影响，但「同一座城被静默挪位置」
 *        与「既有对外数值不许漂移」的纪律相悖，故一并锁。
 *      · 服务端渲染**锚点**下拉（按省市分组，**无 JS 也能选**）；
 *        `assets/astro-place.js` 再取一次 `assets/places-cn.json`（全站共享的静态资源），
 *        把县级并入下拉 ⇒ 基础页面不因 3,210 个选项而膨胀。
 *      · 页面**如实标注**「按 <锚点> 计算，直线距离约 N 千米」——
 *        县级点没有各自预计算的升落，用的是它绑定的锚点，不许把近似说成精确。
 *      · ★ 数据面**反而缩小**：`daily_site` 只被「今天」查询（历史栏读的是 events 表），
 *        而旧滚动窗 −370/+400 天是照抄 `daily` 的窗。观测地 ×8.95 后维持旧窗会得到
 *        262,140 行而**一个查询都用不到** ⇒ 按比例收紧为 **−7 / +120 天**
 *        （＝「免维护窗」4 个月），行数 29,298 → 43,520。
 *
 *   ② 界面：三处去重（页面标题 / 栏目标头 / 板块标题原本都在说「今日天象」）
 *      —— 板块标题改**视觉隐藏**（保大纲与读屏，不占视觉）；
 *      口径与溯源折进 `<details>`；观测地那件事原先在四处各说一遍，合成一句状态行。
 *      表格治本：**外层允许横向滚动 ＋ 表格 min-width ＋ 关键列 nowrap ＋ colgroup 定宽**
 *      （用户截图里「行星天象」被压成竖排四行，成因是表格比容器宽、浏览器压缩中间列）。
 *      触控目标统一 ≥34px，手机上下拉与按钮各占一行撑满。
 *      另补一条「**清除视图参数**」规范链接，收口第十八轮的 F52（URL 污染）。
 *
 *   ③ 审计：`kcj_astro_places_seen` 由「**以日期为键覆盖**、留最近 20 个日期」
 *      改为「**按时间只追加**、留最近 50 次观测」，每条记 (at, date, rows, expect)。
 *      原格式下「**瞬时降级**（一次 0 夹在两次正常之间）」与「**持续为零**」
 *      长得一模一样，而这两种要采取的动作完全相反 ⇒ 有序列才能分辨。
 *      `expect` ＝ 目录声明的锚点总数 ⇒ 站外可判「340/340 齐」还是「38/340 缺」。
 *
 *   无表结构变更（只加索引外的读取面）、无新表 ⇒ **KCJ_ASTRO_SCHEMA 仍为 4**。
 *
 * ── v2.3.3（2026-09-24）：观测地「省份 Tab ＋ 城市网格」
 *   · 340 个锚点在一个原生下拉里，手机上要滚很久才找得到自己那一省
 *     ⇒ 新增 `templates/astro-today.php` 的省份 Tab ＋ 城市格子（切省**纯 CSS**，radio + label）。
 *   · ★ **`<select>` 不删**，改为**视觉隐藏**（`.kcj-astro-sr`）—— 它是三件事的公共支点：
 *     `apply()` 的唯一驱动、`describe()` 的唯一输入、**无 JS 时唯一可用的选择器**。
 *     网格点击不自己算，而是「替读者去动那个 select」（`value` ＋ 派发 `change`）
 *     ⇒ 复用全部既有逻辑、零新代码路径。
 *   · ★ 格子用 `<button>` 而**非** `<a href="?place=…">`：换城**不换 URL**。
 *     服务端只认 `kcj_*` 参数、**不认 `place`** ⇒ 靠跳 URL 换不出内容；
 *     且 `<a>` 必须 `preventDefault` 才能避免重载，而无 JS 降级会随之一起失效。
 *   · ★ 网格默认打开的那一省 ＝ **当前城所在省**（否则读者一进来看到的格子里
 *     没有正在生效的那一格，界面等于说谎 —— 与 v2.3.0 修过的是同一类错）；
 *     定位结果与下拉改选都会**回写网格**（`syncGrid`）。
 *   · 省份 `:checked` 规则**写死到 39**（省级行政区上限 ＋ 余量），不按当前省数动态生成
 *     —— 少写一条就是「那一省点不开」，而这种错只在特定省份被点到时才现形。
 *   · 无表结构变更、无新表 ⇒ **KCJ_ASTRO_SCHEMA 仍为 4**。
 *     `verify_package.py` 的「CSS 标记 == 头部 Version == KCJ_ASTRO_VER」判据不变。
 *
 * ── v2.3.4（2026-09-24）：修 v2.3.3 的**类名错配** —— 网格样式其实一直没挂上
 *   · ★ 症状（线上肉眼可见）：省份按钮下面，城市**不是多列网格，而是一列竖排**；
 *     高亮的省与展开的省**不是同一个**（如高亮「吉林省」而当前城在广东）。
 *   · 根因：模板输出 `class="kcj-astro-place-panels"`（**复数**），
 *     而 CSS 定义的是 `.kcj-astro-place-panel`（**单数**）⇒
 *     `.kcj-astro-place-panel { display:grid; … }` 整块落在**不存在的元素**上，
 *     按钮失去 `grid-template-columns` ⇒ 退化成块级、每行一个。
 *     `.kcj-astro-place-pane { display:none }` 仍生效，而 `.panels` 无任何约束
 *     ⇒ 34 个 pane 全部按块级叠在文档流里、挤出「看起来像列表其实是断裂视觉」的一长条。
 *   · 修法：CSS 两处 `.kcj-astro-place-panel {` → `.kcj-astro-place-panels {`
 *     （主样式块 + 手机端 `@media` 内的网格列数），**不动** 40 条 `:checked` 规则里
 *     本来就是复数的 `.kcj-astro-place-panels`。
 *   · ★ 教训（已入技能 §七之六十八）：**「类名错配」是纯文本可查的错，却有 24 项本机闸
 *     全都查不出来** —— 因为那些闸查的是「PHP 内部逻辑」与「包内一致性」，
 *     **没有任何一道闸把「模板输出的类名」与「CSS 定义的选择器」对起来看**。
 *   · 无表结构变更、无新表 ⇒ **KCJ_ASTRO_SCHEMA 仍为 4**。
 *
 * ── v2.3.5（2026-09-24）：自动定位加「境外 ＋ 跨省」双守卫（用户报「跳吉林延边」「跳深圳」）
 *   · 症状：① 开代理／在境外网络访问 → 观测地自动跳到「吉林延边朝鲜族州」等边城；
 *           ② 关代理仍跳错（人在揭阳、跳到深圳）。
 *   · 取证：`ipwho.is` 实测返回 country_code=CN / city=Shenzhen / 22.5445,114.0545；
 *           深圳 → 最近锚点正是深圳(4.3 km)；揭阳→深圳 261.7 km。
 *           出口 IP 登记在别省而本人在揭阳，是运营商常见现象（宽带出口在省网节点）。
 *   · 根因：定位接口给的是**访问者出口 IP**，不是本人位置。旧代码不问国别、不问省份、
 *           不比距离 ⇒ 把「最近的锚点」直接当本地结果上报。
 *   · ★ 实测否掉「纯距离阈值」方案：340 锚点真实稀疏区下界 = 阿里 512.6 km（最近邻居）；
 *     而揭阳→深圳 261.7 km、揭阳→广州 320.9 km。想挡住「跳深圳」得把阈值压到
 *     250 km 以下，那阿里的正常用户会被误拦 ⇒ **一个距离数字无法两头兼顾**。改用省份校验。
 *   · 修法（两道守卫 ＋ 一键采用）：
 *     ① **境外守卫**：请求补 `country_code`（ipwho.is 走 fields；ipapi.co/json/ 原生带），
 *        回调带 `country`；非 CN 或未知 ⇒ **不套用**，只提示；
 *     ② **跨省守卫**：载荷新增第 16、17 项（命中锚点所属省的 adcode 与省名），
 *        IP 报的省 ≠ 锚点所在省 ⇒ **不自动改城**，只提示 ＋ 给「仍改用 X」按钮；
 *     ③ 同省 ⇒ 正常套用（「同省内最近」语义站得住）。
 *   · ★ 为什么跨省只提示、不硬拦：出口 IP 与本机不同省既可能是运营商常态、也可能是真异地，
 *     代码分不清 ⇒ **判据不足时把决定权交回读者，而不是猜**。
 *   · 新增 `kcj_astro_place_prov_map()`（锚点 → 省码/省名）；新增 CSS `.kcj-astro-place-adopt`。
 *   · 无表结构变更 ⇒ **KCJ_ASTRO_SCHEMA 仍为 4**。


 * ── v2.3.15（2026-09-25）hreflang 补 article 支路 ────────────────────
 *   问题（用户报）：中文版《立春换岁》与英文版被 Google 视为两篇独立网页。
 *   实测：**线上 22 篇 post，hreflang 条数 = 0**；而语言子页 16 个（3 条）、
 *       父栏目页 8 个（1 条）早已合规 ⇒ 缺口**只在文章页**。
 *   因：`includes/hreflang.php` 首行 `if (!is_page()) return $empty;` ——
 *       文章是 post，`is_page()` 恒 false，整条支路被挡死。
 *       ⇒ **设计与实现对不上**：文件头把「首页、文章、CPT、归档」列为只出
 *         x-default，而这道闸让文章连那一条都没出。
 *   改：
 *     · 新增 case ⓪ article 支路，在 `is_page()` 闸**之前**判 `is_single()`；
 *     · 配对锚＝**分类判侧**（中文版 63494103 / 英文版 63494104）
 *       ＋ **互译元字段 `_kcj_translation_of`**（对手 post id，双向各存）；
 *     · 新增 `register_post_meta` 注册该字段 —— **必须注册，REST 才可写**
 *       （未注册的 meta：PUT 返 200 但**静默丢弃**，本项目已实测）；
 *     · 配对不齐（无对手／对手未发布／对手未回指）⇒ 只出 `x-default` 指自身，
 *       **不出 `zh`** —— 沿用既有「宁缺勿造」纪律（单侧声明会被整体忽略）。
 *   不动：page 支路全部逻辑、canonical/robots 边界、x-default 指中文页的口径。
 *   验收：桩测试 41 项全过（含 page 支路零回归）；线上待验 6 条判据见
 *        docs/hreflang.md。
 *
 * ── v2.3.7（2026-09-24）双语面 hreflang ──────────────────────────────
 *   用户意见③：「规范中英双语 hreflang 标签」。给了三条示例，href 全指
 *   `https://kuangchujia.com`。**校验后按「各指各自语言页」执行，未照抄
 *   href 三条同值的写法** ——
 *
 *   ★ 为什么原稿不能用：hreflang 是**双向契约**。A 页声明 B 是它的英文版，
 *     B 页也须声明 A 是它的中文版；且 `href` 三条同值＝声明「中英两版同 URL」，
 *     Google 判为自指重复、**整组忽略**，还可能反过来污染原页语言判定。
 *
 *   ★ 落地口径（站上实测 319 页后定）：
 *     · 16 个语言子页（8 组 × zh/en）各出三条：`zh-CN` → 中文页、
 *       `en` → 英文页、`x-default` → **中文页**（站点主受众为中文，与 locale 一致；
 *       这是口径选择，非技术必然）。
 *     · 父栏目页／列表页是**双语 Tab 单页**（同 URL 内两面板），不存在 en/zh 两版
 *       ⇒ **只出 `x-default` 指自身**；若强行出 `zh` 亦指自身，等于声明
 *       「本页是中文版」而它同时是英文版，属反向失真。
 *     · 首页／归档页不出 hreflang（不属任何语言组，出了是噪音）。
 *
 *   ★ 配对靠「父子关系 ＋ slug」，**不写死 ID 表**：取当前页 `post_parent`，
 *     在其下找 `slug === 'en'` 与 `slug === 'zh'` 的子页。WordPress 保证
 *     同父同级 slug 唯一 ⇒ 配对是确定性的。写死 `585/592` 会在站点改版时
 *     静默失效（本项目已因此踩过一次：`wp_pub_cite.py` 写死 `PAGE_ID = 29`）。
 *     实测 16 子页分布在 8 个 parent（29/35/74/75/13/53/616/700），
 *     每 parent 下恰一枚 zh、一枚 en，**结论成立**。
 *
 *   ★ 新增 `includes/hreflang.php`（1 文件）；**不写 canonical／robots**
 *     （那是 Rank Math 的面，不越界）；`KCJ_ASTRO_SCHEMA` **仍为 4**（无表变更）。
 *     详见 docs/hreflang.md。
 *
 * ── v2.3.6（2026-09-24）：观测地网格移出首页 ＋ 观测地总览页
 *   ─ 用户令（原话）：「这个界面太长了，不行！…… 首页首屏仅保留『当前选中城市』
 *     以及『按我的位置』按钮。将『两级联动省份标签+城市网格面板』完全移出首页，
 *     单独做成一个独立的 WordPress 页面 …… 在首页的城市名字旁，放一个优雅的小链接：[切换观测地]。」
 *   ─ 实测依据：线上首页 HTML 205,161 字符；其中城市相关（340 <option> / 34 radio /
 *     34 tab / 35 pane / 41,007 字符载荷）约 100 KB、占 49% ⇒ 首屏被城市文本压满。
 *   ─ 改法：① astro-today.php 删掉整块网格，只留 select（视觉隐藏）＋按我的位置＋切换链接；
 *           ② 新增短代码 [astro_places_hub] 与模板 templates/astro-places.php 承载网格；
 *           ③ 两页不同 DOM ⇒ 选择经 localStorage（键 kcj_astro_place）承接，
 *              首页在**定位之前**读它（手动选择优先于自动定位，第③条硬约束）；
 *           ④ buildGrid 一字未改（首页路径不变），另加 bindHubGrid 供独立页复用同一纪律。
 *   ─ ★ 拦下的一项（用户意见④ 伪静态 /sky-today/<城市>/）：会让 341 个城市各自成为
 *     **可索引 URL**，等于把「邝楚嘉在揭阳」结构化公开 —— 与实际居住地口径相悖，故不做。
 *     观测地只存在于 localStorage，**绝不进 URL**。
 *   ─ ★ 不照抄的一项（用户意见② JSON-LD Dataset/Person）：首页 @graph 已有 7 节点、
 *     含 Dataset 与 Person，重复注入会造成同实体冲突。hreflang（意见③）另起 v2.3.7 处理。
 *   ─ CSS：新增 .kcj-astro-place-switch / .kcj-astro-places-hub 家族（网格样式全部沿用旧族）。
 *   ─ KCJ_ASTRO_SCHEMA 不变（仍为 4）；无新增表、无新增查询路径。
 *
 * ── v2.3.8 ／ v2.3.9 ／ v2.3.10 ／ v2.3.11（2026-09-24）：首页瘦身 → 功能退化 → 按需取数恢复
 *   ★ 这是一条**四版连着的链**，每一步的动机都来自前一步的副作用，故合并记述。
 *
 *   ─ v2.3.8：只加观测地总览页的**入口与回链**（首页「切换观测地」链接文案与可达性）。
 *   ─ v2.3.9：**首页瘦身（hub 模式）**。首页 HTML 中「城市相关」占约 49%，
 *       实测降幅 147,616 → 65,104 B（−56%）。做法：hub 模式下首页**只内联当前那 1 座城**，
 *       其余 339 城移到总览页。
 *       ⚠ **但同一版引入了一处行为退化**（当时未察觉）：
 *         · 服务端不知浏览器 localStorage 里的**存档城** ⇒ 首屏三处（下拉值／下拉文案／
 *           状态行）都按「默认观测地」硬渲染；
 *         · JS 侧又把 hub 模式下的**存档回填与自动定位一并关掉**
 *           （原判断 `if (!hubMode) { autoRoots.push(root); }`，理由是「数据不在本页，做了也无处落」）。
 *         ⇒ **读者在总览页选过的城，回首页不再生效**。
 *       ⚠ 且状态行从**待定态**（「正在按访问位置选择…」）改成了**静态断言**
 *         （「当前按「X」计算」）⇒ 读者会以为「我的选择被丢了」。
 *         **性质：不是功能缺失，是「页面在说谎」** —— 最伤的一类。
 *   ─ v2.3.10：**缓存键纳入插件版本号**（`_v` 段）。
 *       病根：改版后页面 transient 命中**上一版**的 HTML，表现为「上传了却不生效」，
 *       且极易被误判为「CDN 缓存」而白查一轮。经线上实证：修后**无需手工清缓存即生效**。
 *       另：`templates/astro-today.php` 把「具名数组 → 紧凑行」抽成公共构造点。
 *   ─ v2.3.11（本版）：**按需取数，恢复原功能且不牺牲瘦身成果**。
 *       用户令（原话）：「寻找其它替代方案，要按原来的功能设计」。
 *
 *   原功能共三件：① IP 定位就地换城；② 存档回填；③ 无 JS 时 select 可选。
 *   **三者的共同依赖**＝首页 island 里必须有「要显示的那座城」的数据行。
 *
 *   ★ 被否掉的两条路（记下来，防以后有人重新想一遍）：
 *     · **改 cookie**：首页走 transient 缓存且**键不含用户维度** ⇒ 服务端读 cookie 会**串号**
 *       （A 读者的存档城被发给 B）；
 *     · **走 `?place=`**：违背 v2.3.3 既定纪律「换城不换 URL」—— 会把观测地**永久留在地址栏**
 *       被收藏与转发，那正是「把居住地结构化公开」的一种形式。
 *     ★ 正确方向是**让数据去找读者**（按需取），而不是**把读者的选择塞进共享缓存或 URL**。
 *
 *   做法（新增公开只读端点，前端按需取那一行；约 240 B／次）：
 *     ① `includes/shortcodes.php`：
 *        · 抽出 `kcj_astro_rows_to_places()` —— 「库表行 → 具名数组」**唯一转换点**；
 *        · 抽出 `kcj_astro_place_to_row()`  —— 「具名数组 → 紧凑 17 项行」**唯一构造点**
 *          （原先只在模板里写；现将新增端点，两处各写一份必然失配）；
 *        · 新增 `kcj_astro_load_place_one($date, $city)` —— **带 WHERE 的精确单城查询**
 *          （不复用全量扫表：为一个城付 340 城的代价不成比例）。
 *     ② `includes/rest-import.php`：新增 `GET /kcj-astro/v1/place?date=…&key=…`。
 *        ★ 这是**全插件唯一 `permission_callback => '__return_true'`** 的端点，故安全面须守住：
 *          · **只读**，且**只读 `daily_site` 一张白名单表**，不接受表名参数；
 *          · `key` 必须在**锚点表**内 ⇒ 不接受任意 city 串；
 *          · `date` 严格 `YYYY-MM-DD` ＋ 真实日历日校验（`strtotime` 回读比对，拒 2026-02-30）；
 *          · **单次只返 1 行、无批量面** ⇒ 不可被用来拖全库。
 *     ③ `templates/astro-today.php`：改调公共构造函数；payload 增 `api`／`date`；
 *        状态行改**待定态**（首屏只说「本页默认按 X 计算」，随后由 JS 按存档改写
 *        —— JS 未启用／取数失败时这句仍然成立，不会变成谎话）。
 *     ④ `assets/astro-place.js`：
 *        · 新增 `addRow()`（把按需取回的行**并进同一份 index/order**，幂等）／
 *          `fetchPlaceRow()`（XHR 取单城行，失败**静默回落**）／
 *          `ensureOption()`（hub 模式下拉里补上该城，免得「卡片与下拉不一致」）／
 *          `hubStatus()`（如实说「已按你的选择显示」或「上次选的是 X，本次未取到」）；
 *        · **撤销** v2.3.9 的 `if (!hubMode)` 入列排除；
 *        · 「按我的位置」按钮**撤销「跳总览页」**，恢复就地定位 —— 判据从 `hubMode`
 *          改为 **`canPin = !!island.api`**（有取数通道就就地做，没有才跳页）；
 *        · 定位段同样恢复 hub 模式：先 `fetchPlaceRow` → `addRow` → `autoApply`。
 *
 *   预期体积：首页 65,104 → 约 65,400 B（**＋0.5%**）—— 瘦身成果保住九成九，原功能全恢复。
 *   ★ 本文件（`assets/astro-place.js`）自本版起纳入**「前端脚本零与号」硬闸**：
 *     全文件不得出现字符「与号（字符码 38）」。查询串连接符改用 `AMP` 常量
 *     （`String.fromCharCode(38)`）。逻辑「与」一律改写为嵌套三元或括号取反。
 *
 *   ─ 无表结构变更、无新表 ⇒ **KCJ_ASTRO_SCHEMA 仍为 4**。
 *
 * ── v2.3.12（2026-09-24，本版）：首页两件学术资产加 Schema（F63）
 *   用户令（原话）：「必须为这两个最珍贵的学术资产加入 Schema 语义标签
 *   （使用 HTML5 的 itemscope 或者是将之前为您编写的 JSON-LD 代码合进插件）。」
 *
 *   背景：首页最底部「Recommended Starting Reading · 入门推荐」列出预印本
 *   （DOI 10.5281/zenodo.22803746）与开放数据集（DOI 10.5281/zenodo.22788686），
 *   但**只有 `<ul class="wp-block-list">` 包着** —— DOM 与 JSON-LD 里都没有语义标记，
 *   AI 爬虫易当「普通友链」丢弃。用户给的示例代码是**示意简写**（`itemtype="https://schema.org"`
 *   与 `href="https://doi.org"`），落地一律改**精确类型**与**真 DOI 链接**。
 *
 *   修法＝**两层都给**（缺一层就有盲区：只做 DOM 则 JSON-LD 无节点；只做 JSON-LD 则
 *   关 JS／不看脚本的爬虫仍读不到）：
 *     ① **正文层**（页面 36 `content.raw`）：两条 `<li>` 加 `itemscope itemtype`
 *        ＋ `itemprop` 子标记，`<a href>` 换真 `https://doi.org/…`。
 *     ② **结构化层**（`includes/rankmath.php`）：新增 `front` 上下文 ＋
 *        `kcj_astro_schema_front_assets()`，把同两件资产并进站点 `@graph`
 *        （`ScholarlyArticle` ＋ `Dataset`）。
 *
 *   ★ 守本文件既有硬约束「**不得为不可见内容声明结构化数据**」：
 *     判据＝**正文里确实出现那两个 DOI 号**（不是看栏目名、不是看 `<li>` 数）。
 *     DOI 是资产本身的永久标识，它出现 ⇔ 资产在该页可见；日后删哪条，对应节点自动不发。
 *     逐件开关：`assets` 传 `array('paper'=>bool,'dataset'=>bool)`。
 *
 *   ★ 为什么类型用 `ScholarlyArticle` ＋ `Dataset`：与既有分工一致（见 rankmath.php 头
 *     三条事实纠偏）——这两类都是 **Rank Math 免费版做不到**、由本插件兜的；不重复输出。
 *     既有那个 `Dataset` 节点是**每日天象数据集**（揭阳·逐日），与这两件不冲突
 *     （`@id` 各带独立 fragment：`#asset-preprint-year-turn` / `#asset-calendar-datasets`）。
 *
 *   ─ 无表结构变更、无新表 ⇒ **KCJ_ASTRO_SCHEMA 仍为 4**。
 *
 * ── v2.3.14（2026-09-25，本版）：首页两件学术资产的 JSON-LD 修 **5 处结构缺陷**
 *   触发：用户以 Google 富结果校验类工具跑首页，**同一节点收到两份互不相同的报告**
 *   （一份说 `description`／`creator`／`license` **「未填写字段」**，另一份说
 *   「字符串长度无效」「应指定 name 或 url」而 `license` 正常）。
 *
 *   ★ 两份报告互相矛盾这一事实本身即判据：**根子不在「字段有没有写」，
 *     而在「字段的写法让工具读不稳」** —— 过度使用**纯 `@id` 引用**。
 *     该写法在规范上合法（`@graph` 内可互相引用），但**校验器不做跨节点解析**，
 *     于是凡引用皆被判「未填写」；两份报告解析深度不同，故结论不同。
 *
 *   修法：**一律内联 `name`／`url`**，让字段自带信息、不依赖解析。
 *
 *   ① `Dataset.creator` 与 `ScholarlyArticle.author`：原 `$who` 的 `@id` 拼成
 *      `#person-author`，**而图中只有 `#person`** ⇒ 悬空引用。已纠正 `@id` 并内联 `name`／`url`。
 *   ② `publisher`：原指向 `https://zenodo.org`，**该 `@id` 图内不存在**且无 `name`
 *      ⇒ 补内联 `name = 'Zenodo'` ＋ `url`。
 *   ③ `isPartOf`：原指 `#webpage`（＝**当前页面**），而页面并非数据集的父容器，语义错位
 *      ⇒ 改指 `#website`（站点为稳定容器），并内联 `name`／`url`。
 *   ④ `Dataset.description`：原 36 字被判「长度无效（过短）」⇒ 扩写至 **153 字**，
 *      交代数据内容、格式与用途（面向检索与复用者，非营销语）。
 *   ⑤ `license` 保持**字符串**形态不变（两份报告中一份读得到、一份读不到，
 *      差异来自工具而非数据；不改动以免引入新变量）。
 *
 *   ★ **不在本主题责任范围内的一处**：`#person` 节点的 `image` 是纯 `@id` 引用 `#logo`，
 *     而 `#logo` 节点**并不存在于 `@graph`**（只作为 `logo` 的内联对象存在）⇒ 同属悬空。
 *     该节点由 **Rank Math 插件自身**生成，**不归主题**，须在插件侧处理或接受。
 *
 *   ─ 无表结构变更、无新表 ⇒ **KCJ_ASTRO_SCHEMA 仍为 4**。
 *   ─ 本地验收：`php -l` 全量 **38 件 FAIL 0** ＋ 桩测试 **8 组断言全绿**
 *     （含负控制：`#person-author` 已从全图消失）。
 *
 * ── v2.3.13（2026-09-24）：今日天象块首加「科学定性句」（F64）
 *   用户令（原话）：「在 ### 今日天象 正下方、口径说明上方，强行置入一句总结性科学定性，
 *   字数控制在 100 字内：『本站所罗列之日、月、五星黄道宿度，乃基于现代天体动力学之星历推算，
 *   旨在还原天体客观物理位置，为历史年代学考证及传统历法"去迷信化"研究提供数理坐标底座。』
 *   AIO/SEO 收益：当未来有 AI 搜索提问『kuangchujia.com 的数据可信吗/是干什么用的？』时，
 *   大模型能百分之百精准、完整地把这段话抽取为回答的摘要，避免 AI 瞎编。」
 *
 *   ★ 为什么放进**模板**而不是首页正文：这句话描述的是「本站罗列的日月五星黄道宿度」，
 *     而**凡渲染 `[astro_today]` 的页面都有这套数据**（首页／天象预告页／观测地页…）。
 *     只写首页 ⇒ 别的页面上同样的问题抽不到答案。放模板 ＝ 哪页有数据哪页就有定性。
 *
 *   ★ 与既有声明的分工（**刻意不重复**）：
 *     · 本句（块**首** `.kcj-astro-verdict`）＝ **是什么** —— 数据性质／方法／用途，
 *       面向「可信吗、干什么用的」；
 *     · 既有 `.kcj-astro-disclaimer`（块**尾**·灰字）＝ **怎么算的** —— DE421／
 *       GB/T 33661／精度 ±1 分钟 ＋ 免责，面向「准不准、能不能当出行依据」。
 *     两者都不涉吉凶，立场无冲突。
 *
 *   ★ 结构位置：`<h3 class="kcj-astro-sr">`（视觉隐藏的页内标题）**之后**、
 *     `.kcj-astro-today-head`（日期 ＋ 观测地）**之前** —— 即「观测数据之前、口径说明之前」，
 *     与用户「标题正下方、口径说明上方」的位置要求一致。
 *   ★ 用 `<p>` 不用 `<blockquote>`：它是**本站自述**，不是在引别人的话。
 *   ★ 净 76 字（守「100 字内」）。
 *
 *   ─ 无表结构变更、无新表 ⇒ **KCJ_ASTRO_SCHEMA 仍为 4**。
 */


if (!defined('ABSPATH')) {
    exit; // 禁止直接访问
}

define('KCJ_ASTRO_VER', '2.3.16');
// ── v2.3.16（导航栏目修复版，2026-09-25）变更摘要 ────────────────
//   背景：全站导航栏消失。定谳＝Seedlet 主题以 has_nav_menu('primary') 为唯一开关决定
//         是否渲染 <nav>；菜单 Primary(id 1359) 的 11 项完好，丢的是「挂到哪个位置」的分配记录。
//         WP.com 的 /wp/v2/.../menus 在本站**不接受任何位置名**（一律 400），
//         /menu-locations 在 Jetpack 站点**未实现**（404）⇒ 通用 REST 修不了。
//   新增：REST 端点 `GET|POST /kcj-astro/v1/nav-menu-locations`（权限 manage_options）
//         GET  → 报告 get_registered_nav_menus() / get_nav_menu_locations() / Polylang 语言，
//                **位置名只能由服务器读出**（Polylang 会把 primary 换成 primary_<lang>），
//                客户端探名会盲扫且污染日志，故必须由服务端报。
//         POST → 把既有菜单挂到既有位置（set_theme_mod），写完**回读复核**，只信回读。
//         ⚠ 只做「把既有菜单挂到既有位置」，不新建/删除菜单、不改菜单项内容。
//   表结构未变 ⇒ KCJ_ASTRO_SCHEMA 仍为 4，升级**无需重新激活**。
// ★ 表结构版本（与插件版本**解耦**）：列集/列宽/索引有任何变更都必须 +1，
//   class-astro-db.php 在 init 上比对 option kcj_astro_schema_version，不符即跑 dbDelta 增量升级。
//   v2 = data_version / dt_model 放宽到 VARCHAR(64)（v1.2.1 修 F22）。
//   v3 = 新增第 4 张表 astro_daily_site（v2.0.0，观测地维度）。
//   v4 = events.time_uncertainty 放宽到 VARCHAR(191)（v2.2.6 修 F27：64 < 真值 159 字节）。
//        ⚠ 只有加宽才发 ALTER；本次是加宽，dbDelta 必发 CHANGE COLUMN（它拿 DESCRIBE 的 Type
//          做全串比较，varchar(64) !== varchar(191)，且 varchar 不命中它的 text/blob 两个例外）。
define('KCJ_ASTRO_SCHEMA', '4');
define('KCJ_ASTRO_PATH', plugin_dir_path(__FILE__));
define('KCJ_ASTRO_URL', plugin_dir_url(__FILE__));

// ── 加载顺序有依赖 ──
require_once KCJ_ASTRO_PATH . 'includes/class-astro-db.php';   // ① 表结构正本
require_once KCJ_ASTRO_PATH . 'includes/activation.php';       // ② 激活/停用/清缓存
require_once KCJ_ASTRO_PATH . 'includes/cpt.php';              // ③ CPT / 分类法 / 元字段 / 模板注入
require_once KCJ_ASTRO_PATH . 'includes/shortcodes.php';       // ④ 三个短代码
require_once KCJ_ASTRO_PATH . 'includes/rest-import.php';      // ⑤ REST 导入端点
require_once KCJ_ASTRO_PATH . 'includes/cron.php';             // ⑥ 定时任务与新鲜度
require_once KCJ_ASTRO_PATH . 'includes/compliance.php';       // ⑦ 免责声明 / 自动内链 / 设置页
require_once KCJ_ASTRO_PATH . 'includes/rankmath.php';         // ⑧ Rank Math 适配与 JSON-LD
require_once KCJ_ASTRO_PATH . 'includes/admin-import.php';      // ⑨ 后台「数据导入」页（v2.2.0）
require_once KCJ_ASTRO_PATH . 'includes/status-beacon.php';     // ⑩ 公开状态信标（v2.2.5 · 免凭据观察后台）
// ⑪ 列宽预算（v2.2.6）：写入前按 **WordPress 自己的口径** 量「值长 vs 列上限」。
//    ⚠ 必须排在 rest-import.php 之后（它调用 kcj_astro_preflight）与 class-astro-db.php 之后
//      （它调用 KCJ_Astro_DB::column_meta / table）—— 顺序错了不会报错，只会静默退化成
//      「拿不到列信息 ⇒ 不预检」，正是本项目反复吃的那种「看着正常其实没生效」。
require_once KCJ_ASTRO_PATH . 'includes/col-budget.php';        // ⑪ 字段长度预检（F27）
require_once KCJ_ASTRO_PATH . 'includes/hreflang.php';         // ⑫ 双语面 hreflang（v2.3.7）

// 激活 / 停用钩子
register_activation_hook(__FILE__, 'kcj_astro_forecast_activate');
register_deactivation_hook(__FILE__, 'kcj_astro_forecast_deactivate');

// 供站点自身或定时任务触发清缓存（外部 Python 进程调不了 WP 的 do_action，见 F9）
add_action('kcj_astro_refresh_cache', 'kcj_astro_forecast_flush_cache');
