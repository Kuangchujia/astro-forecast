# 变更记录 · Changelog

版本号规则：`主.次.修`。
**次版本**变动 = 数据表结构或短代码契约变动（升级需读 `docs/deploy.md`）；
**修版本**变动 = 只改代码或前端，重传插件包即可。

> 本文件面向使用者，只列**影响你的事**。
> 每一个缺陷的完整根因与修法见 [`README-full.md`](README-full.md) 与
> [`docs/feasibility-review.md`](docs/feasibility-review.md)。

---

## v2.3.7 — 2026-09-24

- **新增 `includes/hreflang.php`：双语页面互相声明。** 适用于「同一栏目分中英两个
  URL」的站点（如 `/publications/zh/` ↔ `/publications/en/`）。每页在 `<head>` 输出
  `zh-CN` / `en` / `x-default` 三条 `rel="alternate"`，各指**各自语言的真实 URL**。
- **为什么必须成对。** `hreflang` 是双向契约：A 页声明 B 是它的英文版，B 页也必须
  声明 A 是它的中文版，缺一侧整组作废。而「三条标签 `href` 都写同一个网址」＝
  声明「中英两版同 URL」，会被判为自指重复、整组忽略。本模块按「每页只声明自己
  所在的那一组」实现，天然满足双向。
- **配对按「父页 ＋ slug」自动判定，不写死页面 ID。** 取当前页的父页，在其下找
  `slug === 'en'` 与 `slug === 'zh'` 的子页；两者俱全才算语言对。WordPress 保证
  同父同级 slug 唯一 ⇒ 配对是确定性的。写死 ID 表会在站点改版时静默失效。
- **三类页面三种处理**：① 语言子页 → 三条（`x-default` 指中文页）；② 父栏目页与
  列表页是「同 URL 内双语 Tab 单页」、并非 en/zh 之一 → **只出 `x-default` 指自身**；
  ③ 首页与归档页 → 不出标签。**不覆盖 `canonical` / `robots`**，那是 SEO 插件的面。
- 无表结构变更 ⇒ 数据库 schema 仍为 **4**。

---

## v2.3.6 — 2026-09-24

- **观测地的「省份 Tab ＋ 城市网格」从首页移出，独立成一页。** 首页首屏只留
  「当前观测地（如：揭阳）」＋「按我的位置」＋一个「切换观测地」小链接，
  完整网格搬到独立页（用短代码 `[astro_places_hub]` 建一个页面即可，
  建议路径 `/observatories/`）。
- **为什么移。** 340 个观测地的按钮一次全部渲染进首页 HTML，实测首页 HTML
  205,161 字符里约 100 KB（**49%**）是城市相关标记，且把「今日天象」这个主体
  挤到了很下面。移出后首页只承担「今天是什么天象」。
- **跨页用 `localStorage` 承接，不用 URL 参数。** 在独立页选好观测地，回首页即生效。
  若用 `?place=…`，等于把访客的居住地写进地址栏、进浏览器历史、进服务器日志 ——
  **观测地只应存在于访客自己的浏览器里**。
- **读存档排在自动定位之前**：手动选过的城优先于 IP 定位结果。自动定位拿到结果后
  还会**重读页面上此刻真正生效的城**，避免「显示 A 城、其实按 B 城算」。
- **首页的 `<select>` 仍然在**（视觉隐藏）—— 它是换城、状态说明、无 JS 降级三件事的
  公共支点，只是不再把 340 个格子铺在首页。
- 无表结构变更 ⇒ 数据库 schema 仍为 **4**。

---

## v2.3.5 — 2026-09-24

- **自动定位加双守卫。** 此前开着代理（VPN）时，IP 定位会把你判到代理出口所在城市
  （实测：被定位到深圳），而页面显示的城市与后台实际参与计算的城市可能不一致。
  本版加两道守卫：**① 手动选过的城，自动定位不得覆盖**；**② 定位结果与页面上
  此刻生效的城做一致性校验**，不一致时以页面为准并如实提示。
- **不采用「距离阈值」做误判过滤。** 曾评估过「离得太远就不采纳」，实测否掉：
  阈值对真实长距离出行与代理误判**无法区分**，会把正常的异地访问一并拦下。
  改用「手动优先 ＋ 一致性校验」，只依赖站内已知状态，不猜访客位置。
- 省份校验替代距离判定：定位结果必须能归入既有省级表，否则不采纳。
- 无表结构变更 ⇒ 数据库 schema 仍为 **4**。

---

## v2.3.4 — 2026-09-24

- **修 v2.3.3 的一处样式类名错配：城市网格的样式其实一直没挂上。**
  症状是「省份按钮下面，城市不是多列网格而是一列竖排」，而且**高亮的省与展开的省
  不是同一个**（例如高亮「吉林省」，而你当前城在广东）。
- 根因只有一个字符：页面输出的容器类名是 `kcj-astro-place-panels`（**复数**），
  而样式表里定义的是 `.kcj-astro-place-panel`（**单数**）。两者对不上，
  `.kcj-astro-place-panel { display: grid; … }` 整块就落在了**不存在的元素**上，
  格子失去列数定义 ⇒ 退化成块级、每行一个。
- 修法：样式表两处 `.kcj-astro-place-panel {` 改成 `.kcj-astro-place-panels {`
  （主样式块 + 手机端媒体查询里的网格列数）。**换城行为、数据、`<select>` 支点
  一律未动** —— 本次只改样式选择器。
- 顺带：项目侧新增一道离线校验「模板输出的类名 ↔ 样式表定义的选择器」双向配对闸。
  这个缺陷纯文本即可查出，但此前的校验全都只看「PHP 逻辑」与「包内一致性」，
  **没有任何一道把两边类名对起来看**，所以连着两轮上线都没被发现。

---

## v2.3.3 — 2026-09-24

- **观测地选择器升级为「省份 Tab ＋ 城市网格」。** 340 个预置观测地原先全挤在一个
  原生 `<select>` 里，手机上要滚很久才找得到自己那一省。现在按省分 Tab、同省的城
  以格子一次铺开，切省**零脚本**（纯 CSS，`radio + label`）。
- **`<select>` 没有被删掉，只是视觉隐藏。** 它是三件事的公共支点：换城的唯一驱动、
  「按哪算的」那句说明的唯一输入、以及**禁用 JavaScript 时唯一可用的选择器**。
  点格子只是「替你去动那个 `select`」，所以换城行为、状态提示、手动优先于自动定位
  这些既有逻辑**一条都没改**。
- **换城不改变网址。** 全部观测地数据本就随页面一次送达，换城是纯前端替换文本
  （零请求、零计算）。因此格子用 `<button>` 而不是 `<a href="?place=…">` ——
  后者换不出内容，还会把参数永久留在地址栏。
- **默认展开的那一省 ＝ 你当前所在的那一省**，自动定位与手动改选都会同步回网格，
  不会出现「格子亮在 A 城、上面却写着 B 城」。
- 无表结构变更、无新表 ⇒ 数据库 schema 仍为 **4**。重传插件包即可。

---

## v2.3.2 — 2026-09-24

**开源首发版。** 本版本即本仓库的首个公开发布版本，内容等价于内部线上版 v2.3.2，
另做了两件开源化处理（不影响功能）：

- **站点中立化**：入口脚本的 `--wp-site` 默认值不再写死具体域名，改为空串 ＋ 提示
  （原先的默认值意味着别人 clone 下来不传参数就会把数据推到作者站上）；
  插件 `Plugin URI` 改为本仓库地址。
- **个人面中立化**：默认观测地由作者常住地改为**北京**（中性默认）。
  揭阳仍在 38 城清单内，老键一字未改 —— 删改老键会直接断掉历史数据件与历史 URL。

首发版包含的修补：

- **修 F62 · 结构化数据丢失**：「天象预告」页的 `Dataset` 节点归零。根因是 `hub_page`
  上下文未纳入 JSON-LD 白名单，页面级 Schema 无人输出。
- 补充 `hub_page` 正例与 `hub_page_today_off` 负控制，并修 `php_selftest` 桩两处
  （`is_singular` 白名单化、`get_row` 按 SQL 形状认）。

---

## v2.3.1 — 2026-09-23

- **修 F61 · 导入端点整端点 500**：批量子句的 `ON DUPLICATE KEY UPDATE` 快路径里，
  闭包体内取不到 `$wpdb`（未在闭包作用域内声明 `global`）⇒ 任何一次走该路径的导入都会
  致命错误。**v2.3.0 的数据导入功能实际上不可用。**
- 把这条路径纳入**真跑判据**（此前从未被执行过一次），并加**闭包作用域静态闸**：
  函数体内用了 `$wpdb` 却未在本体取得它即报红。
- `php_selftest` 由 41 项增至 **43 项**（新增 `bulk_upsert` 真跑用例 ＋ 可运行性检查）。

---

## v2.3.0 — 2026-09-23

- **观测地扩到全国**：340 个地级锚点 ＋ 2,870 个县级就近绑定（此前只有 38 城）。
  数据口径随之改为 340 锚点 × 128 天 = 43,520 行。
- **界面去重**：观测地下拉按省市分组，消除重复项。
- **数据导入页**：多了**最省事的一条入库路** —— 后台「数据导入」直接吃 NDJSON，
  **不需要应用密码、不装 WP-CLI**。多行 `ON DUPLICATE KEY UPDATE` 批 1,500 行。
- **审计留史**：保留近 N 次导入记录，栏目提供重置链接。
- ⚠ **本版的数据导入带必崩缺陷（F61）⇒ 必须用 v2.3.1 及以上再导数据。**
- ⚠ **耗时不再随进程数下降**：本机实测硬天花板约 **31 行/秒**（父进程侧串行点，
  不是 CPU 瓶颈，24 逻辑核闲置）。43,520 行约 23 分钟，**加进程无益**。

---

## v2.2.0 — v2.2.7 — 2026-09-23

一条修「写得像但跑不了」的长链，逐版修掉导入器的：失败不发声、行序不合理、
字节列宽缺口、队头阻塞、空态被缓存 12 小时。

- **修 F27 · 列宽按字节而非字符**：`data_version` 等列宽声明与真实数据长度对不上，
  而这批列在 MySQL 里是**按字节**计宽的 —— 中文字符占 3 字节，
  「只数字符」的判据在本案上全绿，于是它漏掉了这个缺陷。
- 判据改为**双口径**：字节溢出可检出 ／ 放宽到 191 不误报。

---

## v2.1.0 — 2026-09-23

- **「历史上今日天象」由只做月食扩到三族自算**（月食 ＋ 流星雨 ＋ 行星天象）。
  起因是一条**产品判据**：只收月食时 288 条摊到 366 个「月-日」上**只覆盖 133 天（36.3%）**，
  232 天打开是空的。扩族后实测 366/366 = 100%。
- **三栏目外壳**：今日 / 未来 / 历史上今日渲染成页内三栏目（纯 CSS 切换、零 `<script>`、
  打印全展开）。刻意不做成菜单。
- **同名天象成组渲染**：≥6 条折成年份清单 ＋ 最近一次明细。
- **日期跳转条**（`?kcj_md=MM-DD`）基准年取 **2028（闰年）** —— 用非闰年算「02-28 加一天」
  会跳到 03-01，于是 `02-29` 既写不出来也跳不过去，而页面上完全看不出来。

---

## v2.0.0 — 2026-09-23

- **观测地维度**：新增 `wp_astro_daily_site` 表（日 × 城），复合唯一键 `(date_str, city)`。
  缺了这个复合键，「同一天的第二座城会覆盖第一座，且不报错」。
- 新增短代码 `[astro_today place]`（含观测地选择器）、`[astro_history_today]`、
  `[astro_forecast_report]`（月/季/年报告 ＋ 下载）、`[astro_forecast_list side]`。
- 新增 `python/build_site.py` / `python/history_events.py` / `python/gen_astro_report.py`。

---

## v1.3.0 — 2026-09-23

- **修 F34—F38 · 结构化数据归属**：面板里选了 `Schema Type = Event`，79 条事件详情页却
  **整页零 JSON-LD**。一路排除后读 Rank Math 免费版源码定谳：它的白名单只认 Article 家族，
  面板这条路根本走不通 ⇒ 改由插件模板直接输出 JSON-LD。
- 同轮揪出「日期为空的 `Dataset`」坏节点（`@id` 以 `-` 结尾、`temporalCoverage` 缺失）。
- 为项目补上**第一套 PHP 层判据**：装便携 PHP CLI ⇒ `php -l` 语法闸 ＋ WordPress 钩子桩测试。
  （此前本机无 PHP，插件代码**从未被执行过一次**，问题只能靠读源码猜。）

---

## v1.2.1 — 2026-09-23

**线上首推修复版。** v1.2.0 真实装到站上之后，第一次推数据**没写进去**。
下面几项都不是读代码能读出来的，是**推着真数据跑一遍**才暴露的：

- **修 F22 · 列宽溢出**：REST 回执 `written:0, failed:1`，而版本号实际 36 字符、列宽只有 32。
  A/B/C 边界实验（36 拒 / 32 进 / 34 拒）才把列宽钉死。
- **修 · 静默吞错**：客户端只读回执里的 `written`，丢掉 `failed` 与 `errors[]` ⇒
  服务端拒写被显示成「写入 0 行」，排查时毫无线索。
- **修 · 升级路径缺失**：`dbDelta` 只在激活钩子跑 ⇒ 插件升级后旧列宽**没有任何机制**被带上来。
  新增表结构版本常量（与插件版本**解耦**）＋ `init` 守卫，重传后首次访问自动 ALTER。
- **修 F24 · 同一 option 两种语义**：插件版本与表结构版本被写进**同一个 option** ⇒
  只要取值撞车，守卫就误判「已升级」，**永久跳过 ALTER**。
- **修 F25／F26 · 升级也会静默失败**：`create()` 跑完 `dbDelta` 就无条件落账 ⇒
  ALTER 失败也被记成「已升级」。改为**逐列实测列宽**，达标才落账；`/health` 暴露实测列宽。
- **新增判据**：列类型／列宽逐列对拍、列宽 ≥ 实测最长值、zip 内容与磁盘逐字节一致。

---

## v1.2.0 — 2026-09-22

- **修 F17 · ΔT 改正缺失**：解析降级路径与历史回推把 UT 儒略日当 TT（JDE）用，**漏 ΔT 改正**。
  历史回推是本模块的招牌功能，而 2026 年的数据测不出这个错。
- **修 F18 · 同一史事两处日期互相矛盾**（差一年半），收敛为单一真值源。
- **修 F20 · 必崩**：`register_post_type()` 被写在激活钩子里 ⇒ CPT 必须在**每个请求**的
  `init` 上注册，否则详情页与归档页**必然 404**、后台菜单消失。
- **修 F21 · 架构冲突**：模板不能放进主题目录（托管环境不可写且会被主题升级覆盖）⇒
  改为「插件内置模板 ＋ `template_include` 注入」，保留主题优先的惯例。
- **幂等性三修**：同日重导会插出第二行、关联表无限追加、成功判据失效。

---

## v1.1.0 — 2026-09-16

首个可运行版本。单点/区间计算、历史回推、四个 REST 端点、六个短代码、
`verify_package.py` 静态校验器。

---

## 关于版本可信度

本项目的每一次版本变动都由**可复跑的闸门**背书，闸门数与判据数随缺陷一同增长。
截止 v2.3.2：本机离线闸门 **六道**、`verify_package.py` **470 项**、负控制 **五十余条**、
`php_selftest.py` **48 项**、线上验收 **九项**。

**但「全绿」不等于「没有问题」** —— 本项目的多数严重缺陷都是在「全绿」状态下被
「推真数据」或「看线上输出」抓出来的。故第七道线上验收单独存在，且**不列为门禁**
（联网项当门禁会天天假红）。
