# 贡献指南 · Contributing

欢迎提 Issue 与 Pull Request。

## 一、这个项目的特殊之处（先读这段）

多数开源项目的贡献门槛是「代码能跑」。**这个项目的门槛是「改动能被验证」**。

原因很实际：这套代码算的是天文历法数据，一旦算错，错误**看起来完全正常** ——
一个日食时刻偏了一小时，页面上照样是一行工整的时间；一个默认值写错，
数据照样入库、页面照样显示。**没有任何表面症状。**

所以本仓库有一条贯穿始终的纪律：

> **不许只改代码不补判据。** 每一处行为改动，都要配一条能证明它生效的判据；
> 每一条判据，都要配一条**被故意写坏、必须报红**的负控制。

一条没有负控制的判据，等于用「全绿」换了个心安 —— 它可能恒真，什么都没检查。

## 二、提 Pull Request 之前

### 2.1 必跑（本机离线，全部要 exit=0）

```bash
python python/compute_sky.py --selftest        # 预期：通过 31 项
python verify_package.py                       # 预期：通过 497 项
python php_selftest.py                         # 预期：通过 54 项
python python/build_dataset.py --selftest      # 预期：通过 51 项
python make_zip.py --selftest                  # 预期：通过 7 项
```

改了天文计算或事件枚举，**再加跑**：

```bash
python python/event_almanac.py --selftest      # 约 2 分钟
python python/event_almanac.py --verify        # 约 1 分钟，与四条权威源对拍
python python/history_events.py --selftest     # 约 3—5 分钟
```

### 2.2 改了插件 PHP

`php_selftest.py` 会跑 `php -l` 语法闸（全部 21 件 `*.php`）＋ WordPress 钩子桩测试。
本机没装 PHP 时该脚本会**直接报错退出**，不会静默跳过 —— 这是刻意的。
便携 PHP CLI 的获取方式见 [`docs/deploy.md`](docs/deploy.md)。

### 2.3 改了数据表结构

**三处必须同改**，漏一处校验器会红：

1. `sql/install_tables.sql`
2. `wp-astro-forecast/includes/class-astro-db.php`（dbDelta 正本）
3. `COL_MAXLEN` 常量（列宽监督表）

`verify_package.py` 会做**列集 ＋ 列类型 ＋ 列宽**三向对拍，
并检查每一列宽 ≥ 该列真实数据的最长值（值从真实数据收集，不从声明自证）。

## 三、判据与负控制怎么写

### 3.1 判据要贴**最终呈现**，不要贴中间形态

这是本项目踩过最多的坑。反例（都是真实发生过的）：

| 写错的判据 | 为什么没抓住缺陷 |
|---|---|
| 「文件里存在 `<table>`」 | 页面骨架在，内容可以是空的 |
| 「块以 `<` 开头」 | 中间形态，与最终渲染无关 |
| 「page id 连号」 | 编号有跳 ≠ 有重复页；判据该落在「同一 slug 命中数」 |
| 「产出值不超过 `COL_MAXLEN`」 | 它上游的 `_clip` 已经先裁过一遍 ⇒ **该判据恒真** |
| 「文中出现了某字符串」 | 描述该判据的**文档本身**会命中，把自己豁免掉 |

正确的不变量通常在上游：`_clip` **从不该需要触发**；一旦触发就是列宽装不下真值，
属于静默数据失真，必须报出来而不是悄悄截掉。

### 3.2 负控制要验**产品里真正跑的那份代码**

把规则**抽成模块级函数**，判据与负控制**调同一份**。
如果在负控制里把正则重抄一遍，证明的只是「我抄的那份会报红」。

### 3.3 数值必须来自实测，不能来自印象

真实事故：行星年率判据按旧注释里的印象估成 `[20, 60]`，实测是 **19.66**
⇒ 该判据会让 126 年全量当场报红、拒绝写盘，白跑约 30 分钟。

## 四、绝对不要做的事

- **不要编造天文数据。** 某个类型一件事件都没有，就让它空着，页面如实显示「暂无」。
  绝不为「让页面有内容」造占位事件。自造的日食时刻看起来很像真的，但错得无法察觉。
- **不要自算日食。** 口径是「照录 NASA GSFC 目录、不自造」（本仓库不附目录）。
- **不要引入第三方历表的复制件。** 计算一律由本仓库代码现场产生；
  与他方历表只能做**内部比对**，不能复制、不能发布、不能随包分发。
- **不要把凭据写进任何文件。** 走环境变量或命令行参数。
  注册/导入 URL 里也**不要**带令牌 —— 那会明文留档在提交里。
- **不要改观测地老键。** `jieyang` / `wulumqi` 这类键**已写进历史数据件与历史 URL**，
  改名即断链。新增可以，改名不行。

## 五、报缺陷时请带上

1. **你看到的现象**（页面截图／命令输出原文）
2. **复跑命令**（能让我重现的那一条）
3. **预期与实际**
4. 若与天文数值有关：**你比对的是哪个权威源**、什么日期、差多少

第四条尤其重要。本项目的取值都以「能落权威源」为准 ——
IMO/RASC（流星雨）、NASA GSFC（日食）、香港天文台与台北天文馆（历法）。
只说「感觉不对」我无法处理；说「与 XX 源差 3 分钟」我就能查。

## 六、风格

- **中文注释、中文文档。** 本项目的读者是非技术背景的中文作者。
- **注释写「为什么」，不写「是什么」。** 代码本身能说明它做什么；
  说明不了的是「为什么非这样不可」——那才是注释该写的东西。
- 提交信息用中文，说清**改了什么、为什么、怎么验证的**。

## 七、许可

提交贡献即表示你同意以本仓库既有许可发布你的贡献：
代码 MIT，数据与文档 CC BY 4.0。
